# Database Configuration
DATABASE_URL=jdbc:postgresql://your-neon-host.neon.tech/your-database?user=your-username&password=your-password&sslmode=require&channelBinding=require

# NATS Configuration
NATS_ENABLED=false

# Server Configuration
PORT=8080

# JWT Configuration (Optional - will use defaults if not set)
JWT_ISSUER=rails-users
# JWT_PUBLIC_KEY_PEM=
# JWT_PRIVATE_KEY_PEM=
# JWT_ACCESS_TTL_SECONDS=900
# JWT_REFRESH_TTL_SECONDS=2592000

# NATS Configuration (Optional - already disabled above)
# NATS_URL=nats://localhost:4222
# NATS_STREAM=rails_events
# NATS_SUBJECT_USER_CREATED=users.user.created
# NATS_SUBJECT_ACCOUNT_CREATED=accounts.account.created

# Accounts gRPC Configuration (Optional - will use defaults)
# ACCOUNTS_GRPC_HOST=localhost
# ACCOUNTS_GRPC_PORT=9090

# Instructions:
# 1. Copy this file to .env
# 2. Replace the DATABASE_URL with your actual Neon database connection string
# 3. Uncomment and set any optional variables you need
